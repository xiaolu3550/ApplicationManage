<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <!-- android:textColorPrimary is the color of the title text in the Toolbar  -->
        <item name="android:textColorPrimary">@android:color/white</item>
        <!-- actionMenuTextColor is the color of the text of action (menu) items  -->
        <item name="actionMenuTextColor">@android:color/white</item>
        <!-- Tints the input fields like checkboxes and text fields -->
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">21dp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="toolBar_pop_item" parent="Base.Theme.AppCompat.Light">

        <item name="android:textColor">@android:color/black</item>
        <item name="overlapAnchor">false</item>
    </style>

    <!--dialog样式背景模糊-->
    <style name="style_dialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!--Dialog按钮样式-->
    <style name="baseresource_style_dialog_button">
        <item name="android:gravity">center</item>
        <item name="android:textSize">14sp</item>
        <item name="bl_corners_radius">20dp</item>
        <item name="bl_stroke_width">1dp</item>
        <item name="bl_unPressed_stroke_color">#F1F1F1</item>
        <item name="bl_pressed_stroke_color">#FF39A0F8</item>
        <item name="bl_unPressed_solid_color">@android:color/white</item>
        <item name="bl_pressed_solid_color">#FF39A0F8</item>
        <item name="bl_unPressed_textColor">#FF474747</item>
        <item name="bl_pressed_textColor">@android:color/white</item>
    </style>

</resources>